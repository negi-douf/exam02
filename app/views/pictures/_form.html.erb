<%= form_for(@picture) do |f| %>

  <%= profile_img(@picture) %>

  <%= f.file_field :image %>
  <br>
  <%= f.label :description %>
  <%= f.text_field :description, class: "form-control" %>

  <%= f.submit "投稿", class: "btn btn-primary" %>
<% end %>
