<div class="container">
  <h1>Pictures</h1>

  <% flash.each do |message_type, message| %>
    <div class="alert alert-<%= message_type %>"><%= message %></div>
  <% end %>

  <button type="button" class="btn btn-default", id="new_button"><%= link_to "Picture 投稿", new_picture_path%></button>

  <%= render "pictures" %>
</div>
